__pycache__/
*.pyc
.venv/
venv/

# Datos reales: no se commitean (cifras de presupuesto municipal, repo público)
data/*.xlsx
data/*.xls
~$*.xlsx
db/*.db
db/*.db-journal
exports/
# Copias de los Excel aprobados que suben las areas (ver FORMULARIO7_CARGAS_DIR)
cargas/

# Locales / editor
.DS_Store
*.log
.claude/scheduled_tasks.lock

# Cloudflare Workers Builds (wrangler.toml) necesita package.json
node_modules/
.wrangler/
